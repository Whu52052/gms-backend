# 机器下线多手套损坏原因修复说明

## 问题描述
当机器下线时，如果多个手套（如左手套和右手套）都有损坏但损坏情况不同，系统只能输入一条通用的损坏原因，无法为每个手套分别记录不同的损坏原因。

## 根本原因
在传统的机器下线表单（showMachineForm）中，虽然UI已经为每个SN码提供了独立的损坏状态选择器（.machine-sn-damage）和原因输入框（.machine-sn-reason），但在提交表单时，代码仍然使用全局的 offlineType 和 damageReason 变量，导致所有手套都使用同一个损坏原因。

## 修复方案

### 1. 读取每个SN码的独立损坏信息
在表单提交处理函数中，添加了以下逻辑来读取每个SN码的损坏状态和原因：

`javascript
// Read per-SN damage selections and per-SN damage reasons for offline mode
const snDamageMap = {};
const snReasonMap = {};
if (status === 'offline') {
  document.querySelectorAll('.machine-sn-damage').forEach(el => {
    snDamageMap[el.dataset.sn] = el.value === 'damaged';
  });
  document.querySelectorAll('.machine-sn-reason').forEach(el => {
    if (snDamageMap[el.dataset.sn]) {
      snReasonMap[el.dataset.sn] = el.value.trim() || '损坏';
    }
  });
}
`

### 2. 更新SN注册表时使用独立的损坏原因
修改了SN注册表更新逻辑，为每个SN码使用其独立的损坏状态和原因：

`javascript
// Update SN registry status for each SN code
if (snMap && Object.keys(snMap).length > 0) {
  for (const [invType, sn] of Object.entries(snMap)) {
    if (!sn) continue;
    const eqType = ...;
    const hType = ...;
    if (status === 'online') {
      await this._registerSNChecked(sn, eqType, hType, 'in_use', machineNumber);
    } else if (status === 'offline') {
      // Use per-SN damage status and reason
      const isDamaged = snDamageMap[sn] || false;
      await this._registerSNChecked(sn, eqType, hType, 
        isDamaged ? 'damaged' : 'available', '', 
        isDamaged ? (snReasonMap[sn] || reason || '损坏') : '');
    }
  }
}
`

### 3. 机器记录的offlineReason汇总所有损坏信息
修改了机器离线记录的offlineReason字段，使其包含所有损坏的SN码及其各自的损坏原因：

`javascript
offlineReason: status === 'offline' ? (Object.values(snDamageMap).some(v => v)
  ? Object.entries(snDamageMap).filter(([,v]) => v)
      .map(([sn]) => sn + '(' + (snReasonMap[sn] || '损坏') + ')')
      .join('; ')
  : reason) : ''
`

## 修复效果

### 修复前
- 机器下线时，所有损坏的手套只能使用一个通用的损坏原因
- 例如：左手套"指尖磨损"，右手套"掌心破裂"，但只能记录为"损坏"

### 修复后
- 每个手套可以有自己的损坏原因
- 机器离线记录会显示：SN-LEFT-001(指尖磨损); SN-RIGHT-001(掌心破裂)
- SN注册表中每个SN码都有独立的damageReason字段

## 测试验证
运行现有测试套件确认修复没有破坏其他功能：
`ash
node test_comprehensive.js
`
结果：33个测试全部通过 ✅

## 相关文件
- js/app.js - 主要修复文件
  - showMachineForm() 方法 - 表单提交处理
  - _onMachineStatusChange() 方法 - UI控制（已正确隐藏全局损坏原因输入框）
  - _updateMachineSNFields() 方法 - SN码字段构建（已提供独立的损坏选择器）

## 注意事项
1. 快速切换模式（_showQuickToggleForm）之前已经正确实现了此功能
2. 本次修复使传统机器下线表单与快速切换模式保持一致
3. 修复向后兼容，不影响已有的数据记录
