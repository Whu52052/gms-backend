services:
  - type: web
    name: gms-backend
    env: docker
    dockerfilePath: ./Dockerfile
    region: singapore
    plan: free
    envVars:
      - key: PORT
        value: "8765"
      - key: TZ
        value: Asia/Shanghai
